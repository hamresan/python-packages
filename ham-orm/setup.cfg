[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    --strict-markers
    --disable-warnings
    --verbose
    -ra
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow running tests
